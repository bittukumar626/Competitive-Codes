//This code is used to reverse the string.
#include<bits/stdc++.h>

using namespace std;

int main()
{
    string s;
    cin>>s;
    string k=s;
    reverse(k.begin(),k.end());
    cout<<k<<endl;
    return 0;
}
